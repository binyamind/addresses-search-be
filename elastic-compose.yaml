version: '3'
services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.13.2
    container_name: es01
    networks:
      - elastic
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=false
    volumes:
      - esdata:/usr/share/elasticsearch/data 
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200"]
      interval: 30s
      timeout: 10s
      retries: 5

  kibana:
    image: docker.elastic.co/kibana/kibana:8.0.0
    container_name: kib01
    networks:
      - elastic
    depends_on:
      - elasticsearch   
    ports:
      - "5601:5601"
    environment:
      - SERVER_NAME = localhost
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - ELASTICSEARCH_USERNAME=${ELASTIC_SEARCH_USERNAME}
      - ELASTICSEARCH_PASSWORD=${ELASTIC_SEARCH_PASSWORD}
    volumes:
      - kibanadata:/usr/share/kibana/data
 

networks:
  elastic:
    driver: bridge

volumes:
  esdata:  # Define the Elasticsearch data volume
  kibanadata:  # Define the Kibana data volume

# TODO need to add username password auth